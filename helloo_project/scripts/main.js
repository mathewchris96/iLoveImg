// main.js
// This script is designed to log a message to the console when the page has fully loaded.
// It adds an event listener to the window object for the 'load' event.

window.addEventListener('load', function() {
    // Log a message to the console to indicate the page has loaded successfully.
    console.log('Page loaded successfully');
});